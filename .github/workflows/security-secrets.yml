name: security-secrets

# AGENTS.md §5.1 — "Jamais de secret dans Git." This job is the automated half of
# that rule; GitHub push protection (repo setting) is the other half.
#
# The gitleaks binary is pinned by version AND verified by SHA-256 rather than
# run through the marketplace action: the action requires a licence key for
# organisation-owned repositories, which would mean storing a secret to scan for
# secrets. Bumping the version means updating both constants below.

on:
  pull_request:
  push:
    branches: [main, pre]

permissions:
  contents: read

env:
  GITLEAKS_VERSION: "8.30.1"
  GITLEAKS_SHA256: "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb"

jobs:
  gitleaks:
    name: no secret in git history
    runs-on: ubuntu-latest
    steps:
      # actions/checkout v7.0.1
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          # Full history: a secret removed from the tip is still leaked.
          fetch-depth: 0

      - name: Install gitleaks (pinned, checksum-verified)
        run: |
          set -euo pipefail
          archive="gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          curl -fsSL -o "$archive" \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/${archive}"
          echo "${GITLEAKS_SHA256}  ${archive}" | sha256sum -c -
          tar -xzf "$archive" gitleaks
          ./gitleaks version

      - name: Scan repository
        # --redact keeps any finding out of the CI logs (AGENTS.md §6.3).
        run: ./gitleaks detect --source . --redact --no-banner --verbose
